{# templates/snapshot.njk #}
<div class="snap-card">
  <div class="snap-value">{{ value }}</div>
  <div class="snap-label">{{ label }}</div>
  {% if caption %}<p class="snap-caption">{{ caption }}</p>{% endif %}
</div>
